// API base URL. Empty string = same origin (dashboard served by the FastAPI app itself).
// Override for local dev if serving the frontend separately from the API, e.g.:
//   const API_BASE = "http://localhost:8000";
const API_BASE = "";
